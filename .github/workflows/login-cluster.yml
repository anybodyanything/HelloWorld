name: Login gcloud cluster
run-name: Login gcloud cluster
on:
  repository_dispatch:
    workflows: ["Package and push image"]

Jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Print string
        run: |
          echo "${{ github.event.client-payload.test_sentence }}"

